#!/bin/sh
echo "==> Running pre-commit checks (gradlew check)..."
./gradlew check
if [ $? -ne 0 ]; then
    echo "❌ Pre-commit checks failed! Please fix errors before committing."
    exit 1
fi
echo "✅ Pre-commit checks passed!"
